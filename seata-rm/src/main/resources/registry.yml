registry:
  # file 、nacos 、eureka、redis、zk、consul、etcd3、sofa
  type: eureka
  eureka:
    service-url: http://localhost:7901/eureka
    # 通过这里去eureka 找服务名
    application: seata-server-01
    weight: 1
#config:
#  # file、nacos 、apollo、zk、consul、etcd3
#  type: file
#  file:
#    name: file.yml